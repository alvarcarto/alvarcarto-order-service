const _ = require('lodash');

function savePrintmotorEvent(payload) {
  const { eventType } = payload;
  // TODO: save event to db
  //       act based on the webhook
  return
}

module.exports = {
  saveEvent,
};
